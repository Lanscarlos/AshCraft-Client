<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="top.lanscarlos.ashcraft">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".AshCraftContext"
        android:allowBackup="true"
        android:icon="@mipmap/ic_logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.AshCraft"
        tools:ignore="Instantiatable">
        <activity
            android:name=".ui.order.OrderActivity"
            android:exported="false" />
        <activity
            android:name=".ui.order.OrderOverviewActivity"
            android:exported="false" />
        <activity
            android:name=".ui.commodity.CommodityActivity"
            android:exported="false" />
        <activity
            android:name=".ui.log.RegisterActivity"
            android:exported="false" />
        <activity
            android:name=".ui.log.LoginActivity"
            android:exported="false" />
        <activity
            android:name=".ui.search.SearchResultActivity"
            android:exported="false" />
        <activity
            android:name=".ui.search.SearchPreviewActivity"
            android:exported="false"
            android:launchMode="singleTask" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>